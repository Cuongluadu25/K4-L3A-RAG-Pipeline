# Individual contribution report

## Thông tin

- Họ và tên: Lê Đức Tùng
- Mã học viên: 2A202603005
- Nhóm: K4-L3A
- Repository/branch: `Cuongluadu25/K4-L3A-RAG-Pipeline` - `agents/task10-role-assignment-guidance`
- Role theo `TEAMMATES.md`: Generation & UI — Task 10, `app.py`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 10 — Generation có citation | Implement `reorder_for_llm()` không đột biến danh sách và giữ nguyên ID; `format_context()` đưa title/source vào context; `call_llm()` dispatch theo `LLM_PROVIDER` cho OpenAI, Gemini và Anthropic; `generate_with_citation()` trả answer, sources, retrieval source và safe refusal khi không có evidence/provider lỗi | `src/task10_generation.py` | Done |
| Chatbot UI | Tích hợp `generate_with_citation()`, hiển thị câu trả lời, nguồn, retrieval method, score, nội dung chunk và link nguồn; thêm điều khiển `top_k` và xóa lịch sử chat | `app.py` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Reorder theo chiến lược đưa các chunk ở vị trí xen kẽ về đầu/cuối context và trả về một list mới.
   **Lý do/evidence:** Giảm hiện tượng lost-in-the-middle nhưng vẫn giữ đủ phần tử và ID để đối chiếu citation; test contract xác nhận list gốc không bị sửa và tập ID trước/sau giống nhau.
   **Trade-off:** Thứ tự context khác thứ tự retrieval ban đầu, nên UI vẫn dùng `sources` gốc để giữ đúng ranking và mapping nguồn.

2. **Quyết định:** Khi không có kết quả retrieval, provider lỗi/thiếu API key hoặc provider trả nội dung rỗng, trả thông báo `SAFE_REFUSAL` thay vì để UI crash hoặc bịa câu trả lời.
   **Lý do/evidence:** Phù hợp yêu cầu chỉ trả lời từ context; kết quả vẫn giữ `sources` và `retrieval_source` khi provider lỗi để UI hiển thị bằng chứng retrieval.
   **Trade-off:** Một lỗi cấu hình LLM làm mất câu trả lời sinh tự động, nhưng ưu tiên tính trung thực và khả năng demo ổn định.

## Kiểm thử và kết quả

- Test đã dùng: `python -m pytest tests\test_contracts.py -q`.
- Kết quả: **14 passed, 1 failed**. Test Task10 `test_reorder_is_non_mutating_and_context_contains_source` đạt. Lỗi còn lại thuộc Task6 vì môi trường thiếu package `rank_bm25` (`ModuleNotFoundError`), không liên quan đến các file tôi phụ trách.
- Kiểm tra contract bằng code: output generation có `answer`, `sources`, `retrieval_source`; `format_context()` chứa `Title` và `Source`; UI gọi pipeline với `top_k` và render sources.
- Lỗi đã xử lý trong phần phụ trách: provider/API lỗi không làm Streamlit crash; câu trả lời rỗng được chuyển thành safe refusal.

## Điều còn hạn chế

- Citation hiện được yêu cầu qua system prompt và context label `[Document N]`; chưa có bước hậu kiểm tự động để từ chối câu trả lời chứa citation không tồn tại trong `sources`.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện là thêm parser/validator citation sau khi gọi LLM và bổ sung test mock cho ba provider, không gọi API thật.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-20
- Tên thành viên: Lê Đức Tùng
